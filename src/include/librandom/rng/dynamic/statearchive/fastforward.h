#include "statearchive.h"

template<class Rng>
class RngFastForward {
    public:
    void fastforward(Rng& rng, size_t from, size_t to) {
        //StateArchive archive("rngarchive.bin",) /// \todo: filename should depend on rng name
    }
}